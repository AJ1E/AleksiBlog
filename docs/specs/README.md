# Specs 工作流

这里用来记录“比较大的改动”。

小改动可以直接做，例如改错别字、改一段简介、新增一篇普通文章。大改动要先写 spec，例如重做首页、改登录、接入 Beszel、改变部署方式、增加测试/CI。

每份 spec 建议包含：

- 目标：为什么要改。
- 范围：会改哪些页面、文件或模块。
- 不做什么：明确这次不碰的边界。
- 用户体验：改完后用户会看到什么。
- 安全影响：是否涉及密码、token、登录、端口、个人数据。
- 验收标准：怎样算完成。

可以复制 `change-template.md` 开始写。
