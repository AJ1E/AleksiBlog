# Allow the loopback-only notes helper to start exactly one controlled rebuild.
# Do not add wildcards, shells, arguments, or unrelated systemctl actions.
aleksiz ALL=(root) NOPASSWD: /usr/bin/systemctl start aleksiz-notes-sync.service
